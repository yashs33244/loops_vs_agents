diff --git a/app/api/v1/campaigns.py b/app/api/v1/campaigns.py
index 80e98b5..9fdb14b 100644
--- a/app/api/v1/campaigns.py
+++ b/app/api/v1/campaigns.py
@@ -138,7 +138,7 @@ async def finalize_session(
     llm: LLMService = Depends(get_llm_service),
 ):
     chat_service = ChatService(CampaignRepository(db), llm)
-    await chat_service.finalize_session(campaign_id, req.extracted_params, user.id)
+    await chat_service.finalize_session(campaign_id, req.extracted_params)
 
     from app.models.campaign import Campaign
     from sqlalchemy import update
diff --git a/app/api/v1/chat.py b/app/api/v1/chat.py
index 551b5cf..869438e 100644
--- a/app/api/v1/chat.py
+++ b/app/api/v1/chat.py
@@ -50,11 +50,12 @@ async def get_chat_history(
     campaign = await campaign_repo.get_by_id_for_user(campaign_id, user.id)
     if not campaign:
         raise HTTPException(status_code=404, detail="Campaign not found")
+    session = await campaign_repo.get_session(campaign_id)
     return {
         "campaign_id": campaign.id,
         "status": campaign.status,
-        "chat_history": campaign.chat_history,
-        "extracted_params": campaign.extracted_params,
+        "chat_history": session.chat_history if session else [],
+        "extracted_params": session.extracted_params if session else {},
     }
 
 
