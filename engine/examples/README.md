# examples

Example plans for the Graph Harness engine.

`bugfix.json` will hold the paper's 10-node bug-fix DAG used by the end-to-end
demo (spec "Definition of done" #2). It is a placeholder until the scheduler
and CLI are implemented.
